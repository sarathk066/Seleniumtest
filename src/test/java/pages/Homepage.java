package pages;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class Homepage {

    WebDriver driver;

    public Homepage(WebDriver driver) {
        this.driver=driver;

     // Locators for this page
     By homebtn = By.id ("testlogin");

     // method to click on homebutton
      //
    }
}
